# Optional. Only needed if you want pinned tiles to pull artwork from TMDB by
# name rather than from the linked page's own preview image.
#
# A key is free from https://www.themoviedb.org/settings/api
# TMDB_API_KEY="..."
