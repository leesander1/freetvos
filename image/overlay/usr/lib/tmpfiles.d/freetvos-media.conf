# Where USB drives are mounted. Under /run because it is machine-local and
# disappears on reboot, which is exactly the lifetime of a mounted USB stick.
d /run/media           0755 root root -
d /run/media/freetvos  0755 root root -
